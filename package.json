{
  "name": "bluff",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:testnet": "hardhat run scripts/deploy.js --network citreaTestnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network citreaMainnet",
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "build:all": "npm run compile && cd frontend && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.2",
    "hardhat": "^2.28.6",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.2"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1",
    "dotenv": "^16.4.7"
  },
  "directories": {
    "test": "test"
  },
  "description": "bluff. — anonymous confessions on Citrea (Bitcoin L2), tagged across India"
}
